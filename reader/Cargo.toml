[package]
name = "agent-world-reader"
version = "0.2.0"
license = "MIT"
edition = "2024"
publish = false

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
time = { version = "0.3", features = ["formatting", "parsing"] }
libc = "0.2"
