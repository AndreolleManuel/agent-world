{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capability for the Pixel Ops main window; no filesystem, shell, HTTP, or process permissions.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
